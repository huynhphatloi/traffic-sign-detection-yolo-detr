"""Image-level statistics (plan §8.4) — OWNER: Vinh.

TODO (Vinh): implement. Per-split image counts, image-resolution distribution, and
objects-per-image. Read image dimensions from headers (PIL Image.size, no full decode).
Expected outputs: results/eda/{images_per_split,image_resolution,objects_per_image}.png,
                  results/tables/image_statistics.csv
Reuse: src.data.inspect_dataset.{list_images,label_path_for,parse_label_file}.

This file is an intentional stub — see TODO.md.
"""
